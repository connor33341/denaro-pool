# Stellaris Pool Configuration

# Stellaris Node Connection
STELLARIS_NODE_URL=https://stellaris-node.connor33341.dev/

# Pool Wallet Configuration
# IMPORTANT: Generate a wallet address and private key for the pool
# The pool will mine to this address and distribute rewards from it
POOL_WALLET_ADDRESS=your_pool_wallet_address_here
POOL_PRIVATE_KEY=your_pool_private_key_hex_here

# Pool Fee Configuration
POOL_FEE_PERCENT=2.0              # Pool fee percentage (2% default)
FINDER_BONUS_PERCENT=5.0          # Extra bonus for miner who finds the block (5% default)

# Payout Configuration
MIN_PAYOUT_AMOUNT=1.0             # Minimum balance before payout (1 STR default)

# Data Storage
DATA_DIR=./data/pool              # Directory for gzip JSON storage

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Optional: Enable debug mode
# DEBUG=true
